export default function AdicionarFuncionariosModal({ 
  isOpen, 
  onClose, 
  planoId 
}: { 
  isOpen: boolean; 
  onClose: () => void; 
  planoId: string; 
}) {
  return null; // Temporariamente desabilitado para corrigir build
}